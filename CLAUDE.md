# CLAUDE.md

This project keeps its agent grounding in **[AGENTS.md](./AGENTS.md)** — read that first.
It's the single source of truth (what anthill is, where to look, the conventions that
matter, and key commands).

Keeping the content in `AGENTS.md` rather than here means non-Claude agents get the same
grounding, with no duplication to keep in sync.
